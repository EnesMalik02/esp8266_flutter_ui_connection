import 'package:flutter/material.dart';

class PreviousDataPage extends StatelessWidget {
  const PreviousDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold();
  }
}