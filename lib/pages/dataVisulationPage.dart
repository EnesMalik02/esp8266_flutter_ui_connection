import 'package:flutter/material.dart';

class dataVisulationPage extends StatelessWidget {
  const dataVisulationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
